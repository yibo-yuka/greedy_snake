/**
 * Greedy Snake — Frontend Configuration
 * ========================================
 * Backend: https://greedysnake.duckdns.org/api
 */
window.SNAKE_CONFIG = {
  /** Backend REST API base URL. null = offline / local-score-only mode. */
  apiUrl: 'https://greedysnake.duckdns.org/api',
};
